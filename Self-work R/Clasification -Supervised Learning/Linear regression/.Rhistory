datos <- data("mtcars")
describe(datos)
names(datos)
summary(datos)
datos
data("mtcars")
names(data("mtcars"))
datos
summary(data("mtcars"))
datos
library(datasets)
summary(data(mtcars))
describe(data(mtcars))
cars <- mtcars
cars
columnas <- names(cars)
columnas
ajuste_lineal <- lm(mpg~wt, data= cars)
summary(ajuste_lineal)
setwd("~/Documents/Programación/Machine-Learning/Self-work R/Clasification -Supervised Learning/Linear regression")
# necesito saber como obtener los coheficientes de la regresión lineal
ajuste_lineal1$error
# necesito saber como obtener los coheficientes de la regresión lineal
ajuste_lineal1$Error
ajuste_lineal
# necesito saber como obtener los coheficientes de la regresión lineal
ajuste_lineal
# necesito saber como obtener los coheficientes de la regresión lineal
ajuste_lineal$coefficients
# necesito saber como obtener los coheficientes de la regresión lineal
ajuste_lineal$Errors
# necesito saber como obtener los coheficientes de la regresión lineal
ajuste_lineal$Error
predict(ajuste_lineal1, wt)
ajuste_lineal1 <- lm(mpg ~ wt, data = cars)
summary(ajuste_lineal1)
predict(ajuste_lineal1, wt)
predict(ajuste_lineal1, cars$wt)
wt_gorrito <- predict(ajuste_lineal1, cars$wt)
wt_gorrito <- predict(ajuste_lineal1, data.frame(cars$wt))
wt_gorrito <- predict(ajuste_lineal1, data.frame(cars$wt))
wt-gorrito <- predict(ajuste_lineal1, data.frame(cars$wt))
library(datasets)
cars <- mtcars
columnas <- names(cars)
ajuste_lineal1 <- lm(mpg ~ wt, data = cars)
summary(ajuste_lineal1)
8300/64641
